import { TestBed } from '@angular/core/testing';

import { UserProfileDataServiceService } from './user-profile-data-service.service';

describe('UserProfileDataServiceService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));
/*
  it('should be created', () => {
    const service: UserProfileDataServiceService = TestBed.get(UserProfileDataServiceService);
    expect(service).toBeTruthy();
  });*/
});
